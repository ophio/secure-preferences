ext {
    def versionMajor = 0
    def versionMinor = 1
    def versionPatch = 2
    def versionBuild = 0

    libraryVersionCode = versionMajor * 10000 + versionMinor * 1000 + versionPatch * 100 + versionBuild
    libraryVersionName = "${versionMajor}.${versionMinor}.${versionPatch}"
}